using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace CustomEnums
{
   public enum Tool {Locomotion, Tranformation, Examine};
   public enum MenuChoice {Locomotion, Tranformation, Examine, None};
   public enum Controller {Left, Right};
}
